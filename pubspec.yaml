name: desterlib_flutter
description: "A new Flutter project."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  media_kit: ^1.2.1
  media_kit_video: ^1.3.1
  media_kit_libs_video: ^1.0.7
  lucide_icons_flutter: ^3.1.4
  window_manager: ^0.4.3
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  flutter_inappwebview: ^6.1.5
  file_picker: ^10.3.3
  connectivity_plus: ^6.1.1
  http: ^1.2.2
  shared_preferences: ^2.3.2
  flutter_hooks: ^0.20.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Bundled web app assets
  assets:
    - assets/web/
    - assets/web/assets/
